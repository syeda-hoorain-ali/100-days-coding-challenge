// Question 94: Use the .map() method to create a new array that contains the length of each word from an array of words.
const fruits = ['Banana', 'Orange', 'Sapodilla'];
const length = fruits.map(fruit => fruit.length);
console.log(length);
export {};
